#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  int a[n];
  for (int i = 0; i < n; i++) {
    cin >> a[i];
  }

  int mm = INT_MIN;
  int currentDigit = a[0];


  for (int i = 0; i < n; i++) {
    int val = a[i];
    
  }

  return 0;
}